#include <stdio.h>
#include <stdlib.h>

#include "main.h"

int run(int level, char *datetime, char *data)
{
    printf("level: %d\n", level);
    printf("datetime: %s\n", datetime);
    printf("data: %s\n", data);
    printf("hello world\n");

    
    return 0;
}